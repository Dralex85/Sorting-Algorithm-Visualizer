#ifndef UTILS_HEADER
#define UTILS_HEADER


void swapInt(int (&tab)[], int one, int two);
bool verify(int(&tab)[]);
void sleep();
void initSizes(int (&rectSizes)[]);
void shuffle(int (&rectSizes)[]);

#endif